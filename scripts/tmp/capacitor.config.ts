import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'plugin-project-template',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
